version: '0.1'
guardians:
  acala-guardian:
    networkType: acalaChain
    network: dev
    nodeEndpoint: ${NODE_ENDPOINT}

    monitors:
      dex-price-monitor:
        task: dex.pools
        arguments: 
            currencyId:
                - ${TOKEN_A}
        actions:
        - method: "dexPriceChange"