version: '0.1'
guardians:
  - chain: acala
    network: ${NETWORK}
    monitors:
      dex-price-monitor:
        task: dex.pools
        arguments: 
            currencyId:
                - token: ${TOKEN}
        actions:
        - method: dexPriceChange